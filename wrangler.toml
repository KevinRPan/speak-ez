# Cloudflare Pages configuration
name = "speak-sharp"
compatibility_date = "2024-01-01"

# Pages will auto-detect the static assets in root
# Functions live in /functions directory

# Environment variables (set these in Cloudflare dashboard, NOT here)
# GEMINI_API_KEY = "your-key-here"  # Set in dashboard > Settings > Environment variables
